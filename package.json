{
  "name": "lusalink-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Website Lusalink com formulários processados num backend Node.js no Render.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/forms.js"
  },
  "dependencies": {
    "express": "5.2.1",
    "express-rate-limit": "8.6.2",
    "nodemailer": "9.0.4",
    "resend": "6.18.1"
  }
}
