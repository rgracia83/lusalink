services:
  - type: web
    name: lusalink
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: EMAIL_PROVIDER
        value: resend
      - key: RESEND_API_KEY
        sync: false
      - key: FORM_TO_EMAIL
        value: info@lusalink.pt
      - key: FORM_FROM_EMAIL
        sync: false
      - key: FORM_SUBJECT_PREFIX
        value: Lusalink
